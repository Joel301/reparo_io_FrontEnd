const initialState = {
  clients: [],
  client: [],
};

const clientReducer = (state = initialState, action) => {
    switch (action.type) {
      
      default:
       return state;
    }

}

export default clientReducer; 
